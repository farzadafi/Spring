version: '3.7'

services:
  currency-exchange:
    image: currency-exchange-service
    ports:
      - "8000:8000"
    networks:
      - currency-network
    depends_on:
      - naming-server
    environment:
      EUREKA.CLIENT.SERVICEURL.DEFAULTZONE: http://naming-server:8761/eureka

  naming-server:
    image: naming-server
    ports:
      - "8761:8761"
    networks:
      - currency-network

networks:
  currency-network: